# Docker Environment Configuration for Hudu MCP Server
# Copy this file to .env for docker-compose or pass these variables to docker run

# Hudu Configuration (Required)
HUDU_BASE_URL=https://your-hudu-instance.com
HUDU_API_KEY=your-hudu-api-key-here
HUDU_TIMEOUT=30000

# Application Configuration
NODE_ENV=production
LOG_LEVEL=info

# Optional: If using HTTP transport instead of stdio
MCP_SERVER_PORT=3000